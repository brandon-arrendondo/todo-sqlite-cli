/target
*.db
*.db-journal
*.db-wal
*.db-shm
.todo-sqlite-cli
THIRD_PARTY_LICENSES.txt
*.cdx.json
__pycache__/
*.pyc
.venv/
mcp_server/build/
mcp_server/*.egg-info/

# MQTT coordinator/worker sync (mcp_server/): connection config (host/port/
# client-id/etc — the password itself is never stored here, only an env var
# name, but the file is still node-local config, not something to share via
# git) and the coordinator's on-disk pending-request queue.
*mqtt*.json
*.mqtt-pending.json
